{
  "name" : "extract_professionalclaimline",
  "type" : "Microsoft.DataFactory/factories/dataflows",
  "apiVersion" : "2018-06-01",
  "properties" : {
    "type" : "MappingDataFlow",
    "folder" : {
      "name" : "Wellfargo0824/EXTRACTS/PACKAGES"
    },
    "typeProperties" : {
      "sources" : [ {
        "name" : "ProfessionalCLaimLine",
        "dataset" : {
          "referenceName" : "genric_parquet",
          "type" : "DatasetReference"
        }
      } ],
      "sinks" : [ {
        "dataset" : {
          "referenceName" : "Generic_File_TGT",
          "type" : "DatasetReference"
        },
        "name" : "ProfessionalClaimlineFile"
      } ],
      "transformations" : [ ],
      "script" : "parameters{\n\tDBName as string (),\n\tDBPassword as string (),\n\tDBUserName as string (),\n\tEmailBody as string (),\n\tEndDate_Adhoc as timestamp (),\n\tEnvironment as string (),\n\tFileDate as string (),\n\tFileNameFromLoop as string (),\n\tFromEmail as string (),\n\tIncremental as string (),\n\tMaxDate as timestamp (),\n\tMaxDate_Adhoc as timestamp (),\n\tMaxDate_String as string (),\n\tMinDate as timestamp (),\n\tMinDate_Adhoc as timestamp (),\n\tMinDate_String as string (),\n\tMoveFileToOutboundFolder as string (),\n\tPharmacyFilename as string (),\n\tPharmacyFilePrefix as string (),\n\tProfessionalClaimLineFilename as string (),\n\tProfessionalCLaimLineFilePrefix as string (),\n\tProfessionalHeaderClaimFileName as string (),\n\tProfessionalHeaderClaimFilePrefix as string (),\n\tRootPathForFileCreation as string (),\n\tRS_Pharmacy as integer (),\n\tRS_ProfessionalClaimHeader as integer (),\n\tRS_ProfessionalClaimLine as integer (),\n\tServerName as string (),\n\tSmtpServer as string (),\n\tStagingFolder as string (),\n\tStagingPath as string (),\n\tStartDate_Adhoc as timestamp (),\n\tSubject as string (),\n\tToEmail as string (),\n\tTransmission_FileName as string (),\n\tTransmission_FileName_Prefix as string (),\n\tZipFile as string (),\n\tZipFolder as string ()\n}\n\nsource(output(\n\t\tModifier_4 as string,\n\t\tClaim_Line as string,\n\t\tProcedure_Code as string,\n\t\tDiagnosis_Code_Pointer as string,\n\t\tModifier_1 as string,\n\t\tModifier_2 as string,\n\t\tEmergency_Indicator as string,\n\t\tModifier_3 as string,\n\t\tClaim_ID as string,\n\t\tBilled_Units as string,\n\t\tLine_Charge as string,\n\t\tPlan_Paid as string,\n\t\tPlace_of_Service as string,\n\t\tProvider_ID as string,\n\t\tCovered_Units as string,\n\t\tMember_Responsibility as string,\n\t\tAllowed_Amount as string,\n\t\tCapitation_Indicator as string,\n\t\tService_To_Date as string,\n\t\tMember_Liability as string,\n\t\tService_From_Date as string,\n\t\tID_Qualifier as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tisolationLevel: 'READ_UNCOMMITTED',\n\tquery: 'SELECT RTRIM(LTRIM([Claim_ID])) AS [Claim_ID],RTRIM(LTRIM([Claim_Line])) AS [Claim_Line],RTRIM(LTRIM([Service_From_Date])) AS [Service_From_Date],RTRIM(LTRIM([Service_To_Date])) AS [Service_To_Date] ,RTRIM(LTRIM([Place_of_Service])) AS [Place_of_Service],RTRIM(LTRIM([Emergency_Indicator])) AS [Emergency_Indicator],RTRIM(LTRIM([Procedure_Code])) AS [Procedure_Code], RTRIM(LTRIM([Modifier_1])) AS [Modifier_1],RTRIM(LTRIM([Modifier_2])) AS [Modifier_2],RTRIM(LTRIM([Modifier_3])) AS [Modifier_3],RTRIM(LTRIM([Modifier_4])) AS [Modifier_4], RTRIM(LTRIM([Diagnosis_Code_Pointer])) AS [Diagnosis_Code_Pointer],RTRIM(LTRIM([Line_Charge])) AS [Line_Charge],RTRIM(LTRIM([Billed_Units])) AS [Billed_Units], RTRIM(LTRIM([ID_Qualifier])) AS [ID_Qualifier],RTRIM(LTRIM([Provider_ID])) AS [Provider_ID],RTRIM(LTRIM([Plan_Paid])) AS [Plan_Paid],RTRIM(LTRIM([Member_Liability])) AS [Member_Liability], RTRIM(LTRIM([Allowed_Amount])) AS [Allowed_Amount],RTRIM(LTRIM([Covered_Units])) AS [Covered_Units],RTRIM(LTRIM([Member_Responsibility])) AS [Member_Responsibility], RTRIM(LTRIM([Capitation_Indicator])) AS [Capitation_Indicator] FROM [EXTRACT].FL_3M_Professional_Claim_line',\n\tformat: 'query',\n\tstaged:false) ~> ProfessionalCLaimLine\nProfessionalCLaimLine sink(input(\n\t\tModifier_4 as string,\n\t\tClaim_Line as string,\n\t\tProcedure_Code as string,\n\t\tDiagnosis_Code_Pointer as string,\n\t\tModifier_1 as string,\n\t\tModifier_2 as string,\n\t\tEmergency_Indicator as string,\n\t\tModifier_3 as string,\n\t\tClaim_ID as string,\n\t\tBilled_Units as integer,\n\t\tLine_Charge as decimal(0,0),\n\t\tPlan_Paid as decimal(0,0),\n\t\tPlace_of_Service as string,\n\t\tProvider_ID as string,\n\t\tCovered_Units as integer,\n\t\tMember_Responsibility as decimal(0,0),\n\t\tAllowed_Amount as decimal(0,0),\n\t\tCapitation_Indicator as string,\n\t\tService_To_Date as string,\n\t\tMember_Liability as decimal(0,0),\n\t\tService_From_Date as string,\n\t\tID_Qualifier as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tsaveOrder: 10000,\n\tmapColumn(\n\t\tModifier_4,\n\t\tClaim_Line,\n\t\tProcedure_Code,\n\t\tDiagnosis_Code_Pointer,\n\t\tModifier_1,\n\t\tModifier_2,\n\t\tEmergency_Indicator,\n\t\tModifier_3,\n\t\tClaim_ID,\n\t\tBilled_Units,\n\t\tLine_Charge,\n\t\tPlan_Paid,\n\t\tPlace_of_Service,\n\t\tProvider_ID,\n\t\tCovered_Units,\n\t\tMember_Responsibility,\n\t\tAllowed_Amount,\n\t\tCapitation_Indicator,\n\t\tService_To_Date,\n\t\tMember_Liability,\n\t\tService_From_Date,\n\t\tID_Qualifier)) ~> ProfessionalClaimlineFile"
    }
  }
}