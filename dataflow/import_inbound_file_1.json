{
  "name" : "import_inbound_file_1",
  "type" : "Microsoft.DataFactory/factories/dataflows",
  "apiVersion" : "2018-06-01",
  "properties" : {
    "type" : "MappingDataFlow",
    "folder" : {
      "name" : "Wellfargo0824/EXTRACTS/PACKAGES"
    },
    "typeProperties" : {
      "sources" : [ {
        "name" : "FlatFileSource",
        "dataset" : {
          "referenceName" : "Generic_File_SRC",
          "type" : "DatasetReference"
        }
      } ],
      "sinks" : [ {
        "dataset" : {
          "referenceName" : "genric_parquet",
          "type" : "DatasetReference"
        },
        "name" : "OLEDBDestination"
      } ],
      "transformations" : [ ],
      "script" : "parameters{\n\tAdHoc_Inbound as string (),\n\tAdHoc_IND as boolean (),\n\tAET_EXCEL_FILENAME as string (),\n\tAET_EXCEL_FILEPATH as string (),\n\tAHC_EXCEL_FILENAME as string (),\n\tAHC_EXCEL_FILEPATH as string (),\n\tEmail_Recipients as string (),\n\tHUM_EXCEL_FILENAME as string (),\n\tHUM_EXCEL_FILEPATH as string (),\n\tINBOUND_FILENAME as string (),\n\tINBOUND_FILEPATH as string (),\n\tLHC_EXCEL_FILENAME as string (),\n\tLHC_EXCEL_FILEPATH as string (),\n\tOUT_FILEPATH as string (),\n\tTEMPLATE_PATH as string (),\n\tUHC_EXCEL_FILENAME as string (),\n\tUHC_EXCEL_FILEPATH as string ()\n}\n\nsource(output(\n\t\t{Enrollment Parish} as string,\n\t\t{Client CIN} as string,\n\t\t{Enrollment Region} as string,\n\t\t{Prior Plan} as string,\n\t\t{Managed Care Start Date} as string,\n\t\tClient as string,\n\t\tPlan as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> FlatFileSource\nFlatFileSource sink(input(\n\t\tEnrollment_Parish as string,\n\t\tClient_CIN as string,\n\t\tEnrollment_Region as string,\n\t\tPrior_Plan as string,\n\t\tManaged_Care_Start_Date as string,\n\t\tClient as string,\n\t\tPlan as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false,\n\tstagingSchemaName: 'dbo',\n\tsaveOrder: 10000,\n\tinsertable:true,\n\tdeletable:false,\n\tupsertable:false,\n\tupdateable:false,\n\tkeys:[],\n\tmapColumn(\n\t\tEnrollment_Parish = {Enrollment Parish},\n\t\tClient_CIN = {Client CIN},\n\t\tEnrollment_Region = {Enrollment Region},\n\t\tPrior_Plan = {Prior Plan},\n\t\tManaged_Care_Start_Date = {Managed Care Start Date},\n\t\tClient,\n\t\tPlan)) ~> OLEDBDestination\nOLEDBSource sink(input(\n\t\tZip as string,\n\t\t{Procedure Code} as string,\n\t\t{Date of Birth} as string,\n\t\t{Medicaid  ID} as string,\n\t\t{Primary Diagnosis} as string,\n\t\t{Authorization Status} as string,\n\t\tCity as string,\n\t\t{LDH Authorization Types} as string,\n\t\t{Provider NPI} as string,\n\t\t{Provider Name} as string,\n\t\t{Authorization End Date} as string,\n\t\t{Type of Service} as string,\n\t\t{Member First Name} as string,\n\t\t{Authorization Number} as string,\n\t\t{Authorization Start Date} as string,\n\t\t{Units Approved} as integer,\n\t\tState as string,\n\t\t{Member Last Name} as string,\n\t\t{Provider Address} as string,\n\t\tModifier as string,\n\t\t{Provider TIN} as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> ExcelDestination\nOLEDBSource sink(input(\n\t\tZip as string,\n\t\t{Procedure Code} as string,\n\t\t{Date of Birth} as string,\n\t\t{Medicaid  ID} as string,\n\t\t{Primary Diagnosis} as string,\n\t\t{Authorization Status} as string,\n\t\tCity as string,\n\t\t{LDH Authorization Types} as string,\n\t\t{Provider NPI} as string,\n\t\t{Provider Name} as string,\n\t\t{Authorization End Date} as string,\n\t\t{Type of Service} as string,\n\t\t{Member First Name} as string,\n\t\t{Authorization Number} as string,\n\t\t{Authorization Start Date} as string,\n\t\t{Units Approved} as integer,\n\t\tState as string,\n\t\t{Member Last Name} as string,\n\t\t{Provider Address} as string,\n\t\tModifier as string,\n\t\t{Provider TIN} as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> ExcelDestination\nOLEDBSource sink(input(\n\t\tZip as string,\n\t\t{Procedure Code} as string,\n\t\t{Date of Birth} as string,\n\t\t{Medicaid  ID} as string,\n\t\t{Primary Diagnosis} as string,\n\t\t{Authorization Status} as string,\n\t\tCity as string,\n\t\t{LDH Authorization Types} as string,\n\t\t{Provider NPI} as string,\n\t\t{Provider Name} as string,\n\t\t{Authorization End Date} as string,\n\t\t{Type of Service} as string,\n\t\t{Member First Name} as string,\n\t\t{Authorization Number} as string,\n\t\t{Authorization Start Date} as string,\n\t\t{Units Approved} as integer,\n\t\tState as string,\n\t\t{Member Last Name} as string,\n\t\t{Provider Address} as string,\n\t\tModifier as string,\n\t\t{Provider TIN} as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> ExcelDestination\nOLEDBSource sink(input(\n\t\tZip as string,\n\t\t{Procedure Code} as string,\n\t\t{Date of Birth} as string,\n\t\t{Medicaid  ID} as string,\n\t\t{Primary Diagnosis} as string,\n\t\t{Authorization Status} as string,\n\t\tCity as string,\n\t\t{LDH Authorization Types} as string,\n\t\t{Provider NPI} as string,\n\t\t{Provider Name} as string,\n\t\t{Authorization End Date} as string,\n\t\t{Type of Service} as string,\n\t\t{Member First Name} as string,\n\t\t{Authorization Number} as string,\n\t\t{Authorization Start Date} as string,\n\t\t{Units Approved} as integer,\n\t\tState as string,\n\t\t{Member Last Name} as string,\n\t\t{Provider Address} as string,\n\t\tModifier as string,\n\t\t{Provider TIN} as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> ExcelDestination\nOLEDBSource sink(input(\n\t\tZip as string,\n\t\t{Procedure Code} as string,\n\t\t{Date of Birth} as string,\n\t\t{Medicaid  ID} as string,\n\t\t{Primary Diagnosis} as string,\n\t\t{Authorization Status} as string,\n\t\tCity as string,\n\t\t{LDH Authorization Types} as string,\n\t\t{Provider NPI} as string,\n\t\t{Provider Name} as string,\n\t\t{Authorization End Date} as string,\n\t\t{Type of Service} as string,\n\t\t{Member First Name} as string,\n\t\t{Authorization Number} as string,\n\t\t{Authorization Start Date} as string,\n\t\t{Units Approved} as integer,\n\t\tState as string,\n\t\t{Member Last Name} as string,\n\t\t{Provider Address} as string,\n\t\tModifier as string,\n\t\t{Provider TIN} as string\n\t),\n\tallowSchemaDrift: true,\n\tvalidateSchema: false) ~> ExcelDestination"
    }
  }
}